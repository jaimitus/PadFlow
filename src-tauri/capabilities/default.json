{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main PadFlow window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
